import 'package:flutter/material.dart';

class SignIn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFF8F8ED),
      body: Container(
        child: Padding(
          padding: const EdgeInsets.only(top: 50),
          child: Text('Sign in na'),
        ),
      ),
    );
  }
}
